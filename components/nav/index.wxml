<!--components/nav/index.wxml-->
<view class='container'>
  <image class='icon' src="{{lastest?disLeftSrc:leftSrc}}" bind:tap="onLeft"></image>
  <text class="title">{{title}}</text>
  <image class='icon' src='{{first?disRightSrc:rightSrc}}' bind:tap='onRight'></image>
</view>
